# TD-17 — Документация: устаревший HANDOFF, противоречия DEPLOY, канонизация модели коллекций

- **Приоритет:** P3 (доки врут о состоянии системы — дорога для нового инженера/агента начинается с ложных фактов)
- **Усилие:** S (~3–4 ч, редактура)
- **Тип:** docs
- **Зависимости:** после TD-04 (правки DEPLOY-контракта бэкапа — чтобы не редактировать дважды).

## Симптом и влияние

1. **HANDOFF.md** (корень, «Актуально на: 9 августа 2026»):
   - `:26–27` — «основная ветка `agent/production-infrastructure`, HEAD `12ad83a`» — реальность:
     `main` @ `0d6b491`;
   - `:52–62` — перечисляет материалы как «локальные неопубликованные», которые давно в git
     (`docs/superpowers/plans/**`, `scripts/upload-deere-shop-logo.mjs`);
   - `:76` — «39 тестов в Directus/scripts» — сейчас 53 тест-файла только в `directus/`;
   - сам частично заменён более свежим
     `docs/implementation/seo-qa-and-catalog-semantics-handoff-2026-08-24.md`.
2. **DEPLOY.md §7 (~168–180)** — учит настраивать реvalidацию **вручную** через
   «Settings → Webhooks», хотя фактический механизм — управляемый Directus Flow
   (`directus/flows/apply-revalidation-flow.mjs`, `npm run flows:revalidation`,
   `deploy/README.md:50–52`). Две конкурирующие инструкции; ручной путь — устаревший.
   Плюс `:179` — случайный китайский фрагмент «(任何人可以重置缓存)» посреди русского текста.
3. **Модель коллекций описана трижды**: `DIRECTUS_COLLECTIONS_PLAN.md` (корень), `directus/README.md`,
   AGENTS.md §Directus Collections. Сегодня совпадают; каждая правка схемы — три правки доков.
4. `commit-push.bat` (корень, в git) — хардкодит SSH-ключ `C:\Users\Elena\.ssh/jd_landing_deploy`,
   IP VPS и пользователя: машинно-специфичный скрипт в общем репо.

## Решение (минимальное)

1. HANDOFF.md → перенести в `docs/implementation/handoff-2026-08-13.md` с шапкой-дисклеймером
   («архивный снимок на 13.08; актуальные правила — AGENTS.md, актуальный статус — git log»);
   из корня — удалить. В корне README.md уже есть ссылки? — проверить и обновить ссылки.
2. DEPLOY.md §7: переписать на managed-flow-путь (одна инструкция: flows-скрипт + env
   NEXT_REVALIDATE_URL), удалить мануальный-webhook вариант и китайский фрагмент.
3. Канонизация коллекций: canonical — `directus/README.md`; `DIRECTUS_COLLECTIONS_PLAN.md` —
   удалить (содержимое дублируется), AGENTS.md-раздел оставить как краткий перечень со ссылкой
   (в AGENTS.md не переписывать подробно — это правила, не справочник схемы).
4. `commit-push.bat` — убрать из git (`git rm --cached`), оставить локально у владельца; занести
   в `.gitignore`. Это действие затрагивает коллабораторов — согласовать с владельцем перед пушем.

## Подводные камни

1. В `docs/` и AGENTS.md есть ссылки на `HANDOFF.md` (grep перед переносом) — обновить все.
2. Не «регенерировать» HANDOFF заново (это новая работа) — только архивация с дисклеймером.
3. AGENTS.md — правила процесса; правки в нём делать точечно (одна ссылка), не редактируя другие
   секции (ADR-002-дисциплина).
4. `commit-push.bat`: после `git rm --cached` у второго клона файл исчезнет — если скриптом
   пользуется кто-то кроме владельца (уточнить) — тогда параметризовать вместо удаления.

## Allowed files (ADR-002)

- `HANDOFF.md` → `docs/implementation/handoff-2026-08-13.md` (move + шапка)
- `DEPLOY.md` (§7 + фрагмент)
- `DIRECTUS_COLLECTIONS_PLAN.md` (удаление), `directus/README.md` (если надо пометить как canonical)
- ссылки в `README.md`, `AGENTS.md` (точечно), `.gitignore` (+ `commit-push.bat` untrack)

## Верификация

1. `grep -rn "HANDOFF.md\|DIRECTUS_COLLECTIONS_PLAN" --include="*.md" .` (без node_modules) —
   ссылок на старые пути нет.
2. Вычитка DEPLOY.md §7 на соответствие фактическому флоу (сверка с `directus/flows/`).

## Не делаем

- Не переписываем AGENTS.md, не создаём «единую вики», не трогаем SEO-*.md и plan1.md
  (локальные незатреканные заметки — их состояние корректно).
